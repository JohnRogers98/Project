<UserControl x:Class="Project.Views.LogicalFrameControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <ListBox  ItemsSource="{Binding Path=Inputs}" ItemTemplate="{StaticResource inputsListDataTemplate}">

            </ListBox>

            <Grid Grid.Column="2">
                <local:SignalButton Style="{StaticResource outputSignalButtonStyle}">

                </local:SignalButton>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
