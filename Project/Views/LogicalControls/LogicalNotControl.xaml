<UserControl x:Class="Project.Views.LogicalNotControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid>
                <local:SignalButton SignalIsOutput="False" Command="{Binding Path=SelectInputCommand}" 
                                CommandParameter="0" SignalValue="{Binding Path=Inputs[0].SignalValue}"></local:SignalButton>
            </Grid>

            <Grid Grid.Column="2">
                <local:SignalButton SignalIsOutput="True" Command="{Binding Path=SelectOutputCommand}" SignalValue="{Binding Path=Output.SignalValue}"></local:SignalButton>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
